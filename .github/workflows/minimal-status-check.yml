name: 'minimal-status-check'

on:
  pull_request:
    branches: [ main ]
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  minimal-status-check:
    runs-on: ubuntu-latest
    steps:
      - run: echo "✅ Check passed"